---
# Install: ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix
    version: "==2.2.2" # firewalld, sysctl (molecule podman driver dep)
  - name: community.general
    version: "==13.3.0" # verified at first converge (2026-09-08)
  - name: containers.podman
    version: "==1.20.2" # 1.20 removed build.path/build.args - role tasks use the new API
# NOTE: community.docker is deliberately absent - Docker is not installed.

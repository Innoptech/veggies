---
- name: Daemon-reload the gh-runner user manager
  ansible.builtin.systemd:
    scope: user
    daemon_reload: true
  become: true
  become_user: "{{ github_runner_user }}"
  environment:
    XDG_RUNTIME_DIR: "/run/user/{{ github_runner_uid }}"

# Managed by Ansible from secrets/model.yml (vault). Mounted into runner
# containers (EnvironmentFile). Scoped virtual key only - the real Fireworks
# key never touches a runner.
LITELLM_API_BASE=http://host.containers.internal:4000/v1
LITELLM_VIRTUAL_KEY={{ litellm_runner_virtual_key }}

# Managed by Ansible from secrets/model.yml (vault). Mode 0600, no_log.
# These values stay inside this container's environment - nowhere else.
FIREWORKS_API_KEY={{ fireworks_api_key }}
LITELLM_MASTER_KEY={{ litellm_master_key }}

# Managed by Ansible from secrets/infra.yml (vault). Mode 0600.
RESTIC_PASSWORD={{ restic_password }}
AWS_ACCESS_KEY_ID={{ restic_s3_access_key }}
AWS_SECRET_ACCESS_KEY={{ restic_s3_secret_key }}
RESTIC_REPOSITORY={{ backup_repo }}

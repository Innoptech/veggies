# Local development / CI python toolchain.
# Install: python3 -m venv .venv && .venv/bin/pip install -r requirements-dev.txt
# Resolved and pinned 2026-09-03 on Python 3.14.
ansible-core==2.21.3 # >=2.18 required: 2.17.x excluded for CVE-2026-11332
ansible-lint==26.8.0
molecule==26.8.0
molecule-plugins[podman]==26.7.15 # provides the podman driver
pre-commit==4.6.2
yamllint==1.38.0
pytest==9.1.1
